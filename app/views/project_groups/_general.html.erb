<% labelled_tabular_form_for :project_group, @project_group, :url => project_group_path(@project, @project_group), :html => {:method => :put} do |f| %>
    <%= render :partial => 'form', :locals => {:f => f} %>
    <%= submit_tag l(:button_save) %>
<% end %>
